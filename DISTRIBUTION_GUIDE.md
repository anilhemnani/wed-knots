╔════════════════════════════════════════════════════════════════════════════════╗
║                                                                                ║
║           ✅ DISTRIBUTABLE ZIP ARCHIVE - COMPLETE & READY                     ║
║                                                                                ║
║        Maven build generates versioned, self-contained deployment package      ║
║                                                                                ║
╚════════════════════════════════════════════════════════════════════════════════╝


📦 BUILD & DEPLOYMENT
═════════════════════════════════════════════════════════════════════════════════

BUILD COMMAND:
──────────────
mvn clean package

OUTPUT:
───────
target/wed-knots-0.0.1-SNAPSHOT.zip

ARCHIVE STRUCTURE:
──────────────────
wed-knots-0.0.1-SNAPSHOT/          ← Extracted folder (versioned)
├── app/                                  ← Spring Boot JAR
│   └── wed-knots-0.0.1-SNAPSHOT.jar
├── bin/                                  ← Startup scripts
│   ├── start.bat                         ← Windows startup
│   ├── start.sh                          ← Unix/Linux/Mac startup
│   ├── stop.bat                          ← Windows stop
│   ├── stop.sh                           ← Unix/Linux/Mac stop
│   ├── setup.bat                         ← Windows interactive setup
│   └── setup.sh                          ← Unix/Linux/Mac interactive setup
├── config/                               ← Configuration files
│   ├── config.env.example                ← Child config template
│   └── PARENT_config.env.example         ← Parent config template
├── logs/                                 ← Application logs (empty)
├── VERSION.txt                           ← Build information
└── README.md                             ← Deployment guide


═════════════════════════════════════════════════════════════════════════════════

🔄 PARENT/CHILD CONFIG.ENV HIERARCHY
═════════════════════════════════════════════════════════════════════════════════

TWO-LEVEL CONFIGURATION:
────────────────────────

1. PARENT config.env (Optional)
   Location: One level above installation folder
   Example: /apps/config.env (next to /apps/wed-knots-0.0.1-SNAPSHOT/)
   Purpose: Shared defaults across multiple instances
   Loaded: First (provides defaults)

2. CHILD config/config.env (Generated by setup)
   Location: Inside installation folder
   Example: /apps/wed-knots-0.0.1-SNAPSHOT/config/config.env
   Purpose: Instance-specific overrides
   Loaded: Second (overrides parent values)


LOADING ORDER:
──────────────
1. START script loads ../config.env (parent) if present
2. START script loads config/config.env (child) if present
3. Child values override parent values
4. Values passed to Java as system properties


SETUP SCRIPT BEHAVIOR:
─────────────────────
1. Reads parent ../config.env for default values
2. Prompts user interactively with parent values as defaults
3. Writes user responses to config/config.env (child file)
4. User edits/answers appear in child file only
5. Parent file remains unchanged


═════════════════════════════════════════════════════════════════════════════════

📋 EXPECTED ENVIRONMENT VARIABLES
═════════════════════════════════════════════════════════════════════════════════

From application-prod.yml, scripts handle:

SPRING_PROFILES_ACTIVE
  Default: prod
  Purpose: Spring profile selection
  Mapped to: -Dspring.profiles.active

JASYPT_ENCRYPTOR_PASSWORD
  Required: Yes
  Purpose: Master encryption password
  Mapped to: -Djasypt.encryptor.password

DATABASE_URL
  Default: jdbc:postgresql://localhost:5432/wedknots
  Purpose: PostgreSQL connection string
  Mapped to: -Dspring.datasource.url

DATABASE_USERNAME
  Default: wedknots_user
  Purpose: Database username
  Mapped to: -Dspring.datasource.username

DATABASE_PASSWORD
  Required: Yes
  Purpose: Database password (ENC() supported)
  Mapped to: -Dspring.datasource.password

PORT
  Default: 8080
  Purpose: Server port
  Mapped to: -Dserver.port

LOG_LEVEL
  Default: INFO
  Purpose: Root logging level
  Mapped to: -Dlogging.level.root

LOG_FILE
  Default: logs/wedknots.log
  Purpose: Log file path
  Mapped to: -Dlogging.file.name

WHATSAPP_VERIFY_TOKEN
  Optional: Yes
  Purpose: WhatsApp webhook verification
  Mapped to: -Dwhatsapp.webhook.verify-token

WHATSAPP_APP_SECRET
  Optional: Yes
  Purpose: WhatsApp app secret
  Mapped to: -Dwhatsapp.webhook.app-secret


═════════════════════════════════════════════════════════════════════════════════

🚀 DEPLOYMENT WORKFLOW
═════════════════════════════════════════════════════════════════════════════════

STEP 1: EXTRACT ZIP
────────────────────
unzip wed-knots-0.0.1-SNAPSHOT.zip -d /apps/

Result:
  /apps/wed-knots-0.0.1-SNAPSHOT/
  ├── app/
  ├── bin/
  ├── config/
  └── logs/


STEP 2: (OPTIONAL) CREATE PARENT CONFIG
─────────────────────────────────────────
cp /apps/wed-knots-0.0.1-SNAPSHOT/config/PARENT_config.env.example /apps/config.env

Edit /apps/config.env with shared defaults:
  SPRING_PROFILES_ACTIVE=prod
  DATABASE_URL=jdbc:postgresql://db.example.com:5432/wedknots
  DATABASE_USERNAME=wedknots_user
  DATABASE_PASSWORD=secure-password
  JASYPT_ENCRYPTOR_PASSWORD=master-secret
  PORT=8080
  LOG_LEVEL=INFO
  LOG_FILE=logs/wedknots.log

This file is shared by all versions in /apps/ directory.


STEP 3: INTERACTIVE SETUP (FIRST TIME)
───────────────────────────────────────
Windows:
  /apps/wed-knots-0.0.1-SNAPSHOT/bin/setup.bat

Unix/Linux/Mac:
  chmod +x /apps/wed-knots-0.0.1-SNAPSHOT/bin/*.sh
  /apps/wed-knots-0.0.1-SNAPSHOT/bin/setup.sh

Setup will:
1. Load parent /apps/config.env (if exists) for defaults
2. Prompt for each value:
   - SPRING_PROFILES_ACTIVE [prod]:
   - JASYPT_ENCRYPTOR_PASSWORD []:
   - DATABASE_URL [jdbc:postgresql://...]:
   - DATABASE_USERNAME [wedknots_user]:
   - DATABASE_PASSWORD []:
   - PORT [8080]:
   - LOG_LEVEL [INFO]:
   - LOG_FILE [logs/wedknots.log]:
   - WHATSAPP_VERIFY_TOKEN []:
   - WHATSAPP_APP_SECRET []:
3. Write responses to config/config.env


STEP 4: START APPLICATION
──────────────────────────
Windows:
  /apps/wed-knots-0.0.1-SNAPSHOT/bin/start.bat

Unix/Linux/Mac:
  /apps/wed-knots-0.0.1-SNAPSHOT/bin/start.sh

Start script will:
1. Load parent /apps/config.env
2. Load child config/config.env (overrides parent)
3. Stop any existing Java process
4. Create logs/ directory
5. Locate JAR in app/ folder
6. Launch with all environment variables as Java properties


STEP 5: STOP APPLICATION
─────────────────────────
Windows:
  /apps/wed-knots-0.0.1-SNAPSHOT/bin/stop.bat

Unix/Linux/Mac:
  /apps/wed-knots-0.0.1-SNAPSHOT/bin/stop.sh


STEP 6: UPDATE TO NEW VERSION
──────────────────────────────
1. Extract new ZIP:
   unzip wed-knots-0.0.2-SNAPSHOT.zip -d /apps/

2. Run setup (if needed):
   /apps/wed-knots-0.0.2-SNAPSHOT/bin/setup.bat

3. Start new version:
   /apps/wed-knots-0.0.2-SNAPSHOT/bin/start.bat

Note:
- Parent config.env is shared (used by both versions)
- Each version has its own config/config.env (can be different)
- start.bat automatically stops old version before starting new


═════════════════════════════════════════════════════════════════════════════════

📝 CONFIGURATION FILES
═════════════════════════════════════════════════════════════════════════════════

PARENT CONFIG (OPTIONAL):
─────────────────────────
File: /apps/config.env
Location: One level above installation folder
Shared by: All versions in /apps/ directory
Managed by: Administrator
Example content:

  # Parent config - shared defaults
  SPRING_PROFILES_ACTIVE=prod
  DATABASE_URL=jdbc:postgresql://db.example.com:5432/wedknots
  DATABASE_USERNAME=wedknots_user
  DATABASE_PASSWORD=my-secure-password
  JASYPT_ENCRYPTOR_PASSWORD=master-encryption-key
  PORT=8080
  LOG_LEVEL=INFO
  LOG_FILE=logs/wedknots.log


CHILD CONFIG (AUTO-GENERATED):
────────────────────────────
File: /apps/wed-knots-0.0.1-SNAPSHOT/config/config.env
Location: Inside installation folder
Specific to: This version only
Generated by: setup.bat/setup.sh
Managed by: Application administrator
Example content:

  # WedKnots Application - Overrides
  # Generated: Jan 12, 2026 20:10:55
  SPRING_PROFILES_ACTIVE=prod
  JASYPT_ENCRYPTOR_PASSWORD=master-encryption-key
  DATABASE_URL=jdbc:postgresql://localhost:5432/wedknots
  DATABASE_USERNAME=wedknots_user
  DATABASE_PASSWORD=my-secure-password
  PORT=8080
  LOG_LEVEL=INFO
  LOG_FILE=logs/wedknots.log
  WHATSAPP_VERIFY_TOKEN=token123
  WHATSAPP_APP_SECRET=secret456


═════════════════════════════════════════════════════════════════════════════════

🔧 HOW START SCRIPT WORKS
═════════════════════════════════════════════════════════════════════════════════

WINDOWS start.bat FLOW:
───────────────────────
1. Resolve APP_ROOT (installation folder)
2. Check for parent ../config.env
   - If exists: Load all env vars with set command
3. Check for child config/config.env
   - If exists: Load all env vars (override parent)
4. Set defaults for optional vars:
   - SPRING_PROFILES_ACTIVE=prod
   - PORT=8080
   - LOG_FILE=logs/wedknots.log
5. Stop existing java.exe (if running)
   - taskkill /F /IM java.exe
6. Create logs directory (mkdir)
7. Find JAR file in app/ folder
   - dir /b app\wed-knots-*.jar
8. Launch Java with all vars as properties:
   - java -jar app/wed-knots-X.X.X.jar \
     -Dspring.profiles.active=%SPRING_PROFILES_ACTIVE% \
     -Dspring.datasource.url=%DATABASE_URL% \
     -Dspring.datasource.username=%DATABASE_USERNAME% \
     -Dspring.datasource.password=%DATABASE_PASSWORD% \
     -Dserver.port=%PORT% \
     -Dlogging.file.name=%LOG_FILE% \
     -Djasypt.encryptor.password=%JASYPT_ENCRYPTOR_PASSWORD% \
     ... (and others)


UNIX start.sh FLOW:
───────────────────
1. Resolve APP_ROOT (installation folder)
2. Load parent ../config.env with:
   - set -a; source ../config.env; set +a
3. Load child config/config.env with:
   - set -a; source config/config.env; set +a
4. Set defaults using bash defaults:
   - ${VAR:-default_value}
5. Stop existing process:
   - pkill -f "wed-knots-.*\.jar"
6. Create logs directory
7. Find JAR file in app/ folder:
   - ls app/wed-knots-*.jar
8. Launch with exec java \
     -Dspring.profiles.active=$SPRING_PROFILES_ACTIVE \
     ... (all vars as properties)


═════════════════════════════════════════════════════════════════════════════════

🔐 SECURITY NOTES
═════════════════════════════════════════════════════════════════════════════════

Config Files:
  □ Both config.env files contain sensitive data
  □ Add to .gitignore (never commit)
  □ Restrict file permissions: chmod 600
  □ Protect from unauthorized access

JASYPT Encryption:
  □ JASYPT_ENCRYPTOR_PASSWORD is the master key
  □ Keep it very secure
  □ Use for encrypting sensitive properties
  □ DATABASE_PASSWORD can be wrapped in ENC(...)

Parent Config Sharing:
  □ Parent config.env is shared by all versions
  □ Careful when updating shared values
  □ Child config can override for specific versions
  □ Good for multi-instance deployments


═════════════════════════════════════════════════════════════════════════════════

📌 EXAMPLES
═════════════════════════════════════════════════════════════════════════════════

EXAMPLE 1: SINGLE INSTANCE
──────────────────────────
/apps/
├── wed-knots-0.0.1-SNAPSHOT/
│   ├── app/
│   ├── bin/
│   ├── config/
│   │   └── config.env (user-created, instance config)
│   └── logs/

No parent config needed. Child config/config.env has all values.


EXAMPLE 2: MULTIPLE INSTANCES (SAME VERSION)
──────────────────────────────────────────────
/apps/
├── config.env (parent - shared database, Jasypt password, etc.)
├── wed-knots-0.0.1-SNAPSHOT-v1/
│   ├── app/
│   ├── bin/
│   ├── config/
│   │   └── config.env (override PORT=8081)
│   └── logs/
├── wed-knots-0.0.1-SNAPSHOT-v2/
│   ├── app/
│   ├── bin/
│   ├── config/
│   │   └── config.env (override PORT=8082)
│   └── logs/

Parent config has shared: DATABASE_URL, DATABASE_USERNAME, etc.
Each child config overrides PORT for its instance.


EXAMPLE 3: VERSION UPGRADE
──────────────────────────
/apps/
├── config.env (parent - unchanged)
├── wed-knots-0.0.1-SNAPSHOT/    ← OLD version
│   ├── app/
│   ├── bin/
│   ├── config/
│   │   └── config.env
│   └── logs/
├── wed-knots-0.0.2-SNAPSHOT/    ← NEW version
│   ├── app/
│   ├── bin/
│   ├── config/
│   │   └── config.env (setup on new version)
│   └── logs/

Deployment:
1. Extract v0.0.2 zip
2. Run setup on v0.0.2 (uses parent defaults)
3. Start v0.0.2 (automatically stops v0.0.1)
4. Keep v0.0.1 folder for rollback if needed


═════════════════════════════════════════════════════════════════════════════════

✅ SUMMARY
═════════════════════════════════════════════════════════════════════════════════

Distribution Method: Single versioned ZIP archive

Build Command:
  mvn clean package

Output:
  target/wed-knots-0.0.1-SNAPSHOT.zip

Deployment:
  1. unzip wed-knots-0.0.1-SNAPSHOT.zip -d /apps/
  2. (Optional) Create /apps/config.env with shared defaults
  3. Run /apps/wed-knots-0.0.1-SNAPSHOT/bin/setup.bat
  4. Run /apps/wed-knots-0.0.1-SNAPSHOT/bin/start.bat

Features:
  ✓ Self-contained with app, bin, config, logs folders
  ✓ Versioned folder name (easy multi-version deployment)
  ✓ Parent/child config.env hierarchy for flexibility
  ✓ Interactive setup reads parent defaults
  ✓ Start script loads parent then child config
  ✓ Automatic process stopping before restart
  ✓ Works on Windows, Linux, Mac (native scripts)
  ✓ All environment variables from application-prod.yml supported

Security:
  ✓ Jasypt encryption support
  ✓ Master password protection
  ✓ Config files not committed to git
  ✓ File permissions (chmod 600) recommended


═════════════════════════════════════════════════════════════════════════════════

Created: January 12, 2026
Application: WedKnots - Wedding Management System
Version: 0.0.1-SNAPSHOT
Status: Production Ready

